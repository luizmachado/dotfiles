#!/bin/sh
# vim: set filetype=sh :

# Misc
alias gitlog='git log --graph --pretty=format:"%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset" --abbrev-commit'
alias pipupgrade="pip freeze | sed -e 's/==.*//' | xargs pip install --upgrade"
alias pipdeleteall='pip freeze | xargs pip uninstall -y'

# Git
alias gitreset='git add . && git reset --hard'

# Kill
alias killpython='pkill -9 -f python'
alias killfirefox='pkill -9 -f firefox'

# Linux
alias sudo='sudo ' # this is needed to use your aliases with sudo
alias s='sudo'     # this is needed to use your aliases with sudo

# Softwares
alias nano='nvim' # se vira
alias vi='nvim'

alias zs='source ~/.zshrc' # recarregar config rápido

# Tree ignorando .git, __pycache__, node_modules, .venv, dist, .DS_Store
alias tre="tree --prune -a -C -I '__pycache__|node_modules|.git|.venv|dist|.DS_Store|*.egg*|.pytest_cache|.ruff_cache'"

# Tmux
alias makenote="tmux new-window \"~/dotfiles/tmux/scripts/make_note.sh\""

# Clipboard — compatibilidade macOS no Linux (requer xclip)
alias pbpaste='xclip -selection clipboard -o'
alias pbcopy='xclip -selection clipboard -i'

# fabric — patterns customizados
alias cria_commit='fabric --pattern cria_commit'
alias corrige_pt='fabric --pattern corrige_pt'
